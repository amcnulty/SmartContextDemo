import './SideBar.scss';

const SideBar = () => {
    console.log('rendering side bar');
    return (
        <div className='SideBar h-100 border-end'>
            sidebar
        </div>
    );
};

export default SideBar;